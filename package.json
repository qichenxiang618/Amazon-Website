{
  "name": "mern-amazona-14",
  "version": "1.0.0",
  "description": "# Lessons",
  "main": "index.js",
  "engines": {
    "node": "16.16.0"
  },
  "scripts": {
    "build": "cd backend && npm install --force && cd .. && cd frontend && npm install --force && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
